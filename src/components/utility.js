const add = (first, second) => {
    const result = first + second;
    return result;
}

const amarGunfolbetweenTwoNumber = (first, second) => {
    return first * second;
};
export { add, amarGunfolbetweenTwoNumber };